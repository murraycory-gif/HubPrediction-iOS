HUB Predictions — update on the Windows PC, then test in the browser
====================================================================

FIRST TIME (do this once)
-------------------------
1. Stop the old desk: click the terminal that is running npm run dev, press Ctrl+C.
2. Open Command Prompt.
3. Go to the repo folder (the folder that contains hub-prediction):

   cd /d C:\Users\Cory\Developer\HubPrediction-iOS

4. Kalshi keys stay on this PC only. Do not paste PEM in Safari.
   Next to the repo (same folder as hub-prediction), make a .secrets folder:

   HubPrediction-iOS\.secrets\kalshi_key_id.txt
   HubPrediction-iOS\.secrets\kalshi_key.pem

   Or put KALSHI_KEY_ID and KALSHI_PRIVATE_KEY in hub-prediction\.env.local.

5. Double-click update-desk.bat  (or run the one-liner below).
6. On this PC open Edge or Chrome:  http://localhost:8080
7. Hard-refresh: Ctrl+Shift+R
8. Confirm Live is OFF. P&L should be cash minus deposits (example: $263 − $760 = −$497).
   The lower bets log should list Kalshi fills since the first deposit.

EVERY TIME AFTER I SHIP A CHANGE
--------------------------------
In PowerShell, one line (Ctrl+C the old desk window first).
Use a semicolon — PowerShell does not allow & as a command joiner:

cd C:\Users\Cory\Developer\HubPrediction-iOS; .\update-desk.bat

Then open http://127.0.0.1:8080 — refresh is safe. A host process stays on 8080 even while Vite restarts.
If the address bar still says 18080, that tab now redirects to 8080. Do not use Vite's inner port.
If Chrome says refused, the desk window is closed. Double-click start-desk.bat.

Same pull without the bat:

cd C:\Users\Cory\Developer\HubPrediction-iOS; git fetch origin cursor/desk-pnl-bets-charts-be4f; git checkout -B cursor/desk-pnl-bets-charts-be4f FETCH_HEAD; cd hub-prediction; npm.cmd install; npm.cmd run dev

Already pulled and just want to start without git:

   double-click start-desk.bat

PHONE / iPAD / OTHER COMPUTER — NOT ON HOME WI-FI
-------------------------------------------------
Our own tunnel. No Tailscale. No account. No monthly fee. Kalshi keys stay
on this PC. Soft FAIL port-forward TCP 8080 on the router.

One-time:

1. Double-click start-tunnel.bat  (makes keys in .secrets\wireguard).
2. Install WireGuard on this PC — free, open source, no sign-in:
   https://www.wireguard.com/install/
   Import hub-desk-server.conf and click Activate. Leave it on.
3. On your router: forward UDP 51820 to this PC. Not 8080.
4. On the PC browser, look up your home public IPv4. Put that one line in
   HubPrediction-iOS\.secrets\wireguard\endpoint.txt
   Then run start-tunnel.bat again.
5. iPhone / iPad: App Store → WireGuard (free, no account). AirDrop
   hub-desk-phone.conf or hub-desk-ipad.conf → Add → Create from file →
   Activate.
   Other computer: import hub-desk-other-pc.conf the same way.
6. Start the desk (update-desk.bat). Leave that window open.
7. On the device open http://10.77.0.1:8080

The home PC must stay on, WireGuard Active, and the desk window open.
If your home public IP changes, update endpoint.txt and run start-tunnel.bat
again, then re-import the phone/iPad file.
